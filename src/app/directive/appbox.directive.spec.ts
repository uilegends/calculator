import { AppboxDirective } from './appbox.directive';

describe('AppboxDirective', () => {
  it('should create an instance', () => {
    const directive = new AppboxDirective();
    expect(directive).toBeTruthy();
  });
});
